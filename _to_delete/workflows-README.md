# workflows

**Status:** Not yet implemented — planned for **Phase 5**.

GitHub Actions CI pipeline: tests, Docker build/push to ECR, terraform validate/plan.

This directory is scaffolded now as part of the Phase 1 repository structure so the
full project layout is visible from the start. It will be populated when Phase 5 is
implemented; nothing here is deployed by Phase 1's `terraform apply`.
