# video-processor

**Status:** Not yet implemented — planned for **Phase 2**.

Dockerized Python/FFmpeg application that runs as an ECS Fargate task to transcode video into a requested resolution.

This directory is scaffolded now as part of the Phase 1 repository structure so the
full project layout is visible from the start. It will be populated when Phase 2 is
implemented; nothing here is deployed by Phase 1's `terraform apply`.
